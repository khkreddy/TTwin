# 80 · ISO-GEN and Modify

Frozen Lamport-20 engine is **not rewritten**. New work is a new run id.

## CANDIDATE lifecycle

1. Teacher states an idea (ordinary language).
2. AI maps to a hinge pack (chemistry) or syllabus chapter (phy/bio interim).
3. AI authors one MCQ. Stamp: `serve_eligible: false`, `owner_ratified: false`, spec hash, utc.
4. Tray is local (`ttwin.isogen.tray`). Nothing enters the live exam pool.
5. Promotion is a **human** protocol. The agent cannot execute it.

## Modify (test maker)

ISO-GEN on an **existing** item: rewrite stem **and** all four options; TikZ only if the change needs it. Text-only items included. Session-only. Revert = packed bytes. Credit-excluded.

## Honesty

Olympiad / JEE / MathNet / Phy-500 generation stays bannered until those banks have stems and structured options on TTwin. Until then: curated existing items only, or dark.
