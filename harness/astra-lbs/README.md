# Astra LBS harness

Pack-time compiler for learn-by-solve hints. Instantiation is code. No model call per packed option.

Read `../README_ISOGEN_ASTRA.md`, then `82_LBS_COMPILER.md`.

Live commands stay in `tools/` (`lbs_compile.py`, `lbs_astra_corpus.py`, `join_lbs.py`). `scripts/` here is the snapshot of that system as of this package.

Preserve gold `9701_m16_qp_12:q1` and spectroscopy. Never reprint the clicked option (`data/never_cases.v1.json`).
